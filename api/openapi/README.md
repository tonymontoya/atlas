# OpenAPI

This directory contains the REST API v1 OpenAPI source.

- `atlas-v1.yaml` documents the current read-only scaffold API.

The contract is intentionally narrow. Add endpoints here only when the matching
server behavior and tests exist.
