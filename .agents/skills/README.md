# Agent Skills

Vendored from Tony Montoya's `agentic-engineering` skill distribution at
commit `dfd3e1b`. MIT licensed — see `LICENSE`.

OpenCode and other agents-compatible tools discover these automatically from
`.agents/skills/<name>/SKILL.md`. See the top-level `AGENTS.md` for the main
flow (idea → ship) and how the maintainer uses them.

The repository copy is canonical for work in this repo. To update, refresh
these directories from the upstream distribution (or edit in place and note
the divergence here).
