# Atlas Agent

This directory will contain the Atlas Agent entrypoint.

The first scaffold intentionally does not implement mutating Agent operations. Agent mutation work is gated by `dev-plans/security_review_checklist.md`.
